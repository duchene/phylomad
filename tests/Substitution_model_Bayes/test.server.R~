renderUI({

	tabPanel("Test statistics",
		       fluidRow(
		       column(3, checkboxGroupInput("testStats", label = h3("Select the test statistics to be used"),
		       choices = list("Multinomial statistic" = 1, "Chi-Squared statistic" = 2), selected = 1)
		       )
		       )
	
	
	)	


})