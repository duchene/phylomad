library(shiny)

# Define server logic required to draw a histogram
shinyServer(function(input, output) {

  output$modeltestcode <- renderText({
  	source(paste0("tests/", input$modeltotest, "/test.server.R"), local = T)
  })

  })